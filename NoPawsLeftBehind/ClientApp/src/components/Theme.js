import { createTheme } from "@mui/material";

export const appTheme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#311b92',
        },
        secondary: {
            main: 'rgba(255,145,0,0.66)',
            dark: '#ffe0b2',
        },
    },
});